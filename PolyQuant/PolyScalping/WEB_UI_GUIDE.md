# 🌐 BTC 스캘핑 봇 웹 UI 가이드

## 개요

실시간으로 봇의 거래 활동을 모니터링할 수 있는 웹 대시보드입니다.

### 주요 기능

- ✅ **실시간 거래 체결 내역** - WebSocket으로 즉시 업데이트
- ✅ **활성 마켓 모니터링** - 현재 추적 중인 마켓과 포지션
- ✅ **봇 제어** - 시작/중지 버튼으로 간편 제어
- ✅ **통계 대시보드** - PnL, 승률, 거래 횟수
- ✅ **이벤트 로그** - 모든 신호와 거래 기록

---

## 🚀 빠른 시작

### 1. 의존성 설치

```bash
pip install -r requirements.txt
```

새로 추가된 패키지:
- `fastapi` - 웹 프레임워크
- `uvicorn` - ASGI 서버
- `websockets` - 실시간 통신

### 2. 웹 서버 실행

```bash
python3 btc_web_server.py
```

출력:
```
INFO:     Started server process
INFO:     Waiting for application startup.
Starting BTC Scalping Web Server...
BTC price tracker started
Orderbook tracker started
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### 3. 브라우저에서 접속

```
http://localhost:8000
```

또는 다른 기기에서 접속 (같은 네트워크):
```
http://YOUR_IP:8000
```

예: `http://192.168.1.100:8000`

---

## 📊 대시보드 구성

### 상단 상태 바

4개의 주요 지표 표시:

1. **Bot Status**
   - 🟢 RUNNING (실행 중)
   - 🔴 STOPPED (중지됨)

2. **BTC Price**
   - 실시간 비트코인 가격
   - Binance/Coinbase 평균

3. **Total PnL**
   - 총 손익 ($)
   - 🟢 수익 / 🔴 손실

4. **Win Rate**
   - 승률 (%)
   - 이긴 거래 / 전체 거래

### 제어 버튼

- **▶ Start** - 봇 시작
- **⏹ Stop** - 봇 중지
- **🔄 Refresh** - 데이터 새로고침
- **연결 상태** - WebSocket 연결 상태 표시

### Active Markets 섹션

현재 추적 중인 마켓 목록:

```
┌─────────────────────────────────────────────┐
│ Bitcoin Up or Down - Jan 20, 1:15-1:30 ET  │
│ btc-updown-15m-1768889700...                │
│                                             │
│ YES Price: 0.565    NO Price: 0.435         │
│                                             │
│ Position: YES 10                            │
│ PnL: +2.5% ($0.25)                          │
│                                             │
│ Time Left: 12m 30s                          │
└─────────────────────────────────────────────┘
```

각 마켓 카드에 표시되는 정보:
- 마켓 제목
- 마켓 ID
- YES/NO 현재 가격
- 보유 포지션 (있는 경우)
- 미실현 손익
- 남은 시간

### Recent Trades 섹션

최근 거래 내역 (최대 50개):

```
ENTER_YES | YES x10 @ 0.450      +$0.00   15:20:35
EXIT      | YES x10 @ 0.525      +$0.75   15:23:12
ENTER_NO  | NO  x10 @ 0.480      +$0.00   15:25:00
EXIT      | NO  x10 @ 0.450      +$0.30   15:27:45
```

각 거래 항목:
- 액션 (ENTER_YES, ENTER_NO, EXIT)
- 토큰 사이드 (YES/NO)
- 거래 수량
- 가격
- 손익 (청산 시)
- 시간

### Event Log 섹션

모든 이벤트 실시간 로그:

```
15:20:35  signal  ENTER_YES - UP predicted | Conf: 65.2%
15:20:36  trade   ENTER_YES YES x10 @ 0.450
15:23:10  signal  EXIT - Take Profit (+3.2%)
15:23:12  trade   EXIT YES x10 @ 0.525
```

---

## 🔄 실시간 업데이트

### WebSocket 연동

WebSocket을 통해 다음 이벤트를 실시간 수신:

1. **거래 체결** (`trade_executed`)
   - 새 거래가 체결될 때마다 즉시 표시
   - 거래 히스토리에 자동 추가

2. **신호 생성** (`signal_generated`)
   - 전략이 신호를 생성할 때마다 알림
   - 이벤트 로그에 기록

3. **마켓 업데이트** (`market_update`)
   - 5초마다 각 마켓 상태 업데이트
   - 가격, 포지션, 남은 시간 갱신

4. **봇 상태** (`bot_status`)
   - 2초마다 봇 전체 상태 업데이트
   - 통계, BTC 가격, 활성 마켓 수

### 자동 재연결

WebSocket 연결이 끊기면:
- 연결 상태 표시가 🔴로 변경
- 3초 후 자동으로 재연결 시도
- 재연결 성공 시 🟢로 복구

---

## 🎛️ API 엔드포인트

웹 UI는 다음 REST API를 사용:

### GET `/api/status`

봇 현재 상태 조회

**응답:**
```json
{
  "running": true,
  "btc_price": 90913.99,
  "active_markets": [...],
  "stats": {
    "total_trades": 15,
    "winning_trades": 10,
    "total_pnl": 12.50,
    "win_rate": 0.667
  },
  "config": {
    "trading_enabled": false,
    "max_concurrent_markets": 2,
    "daily_loss_limit": 50.0
  }
}
```

### GET `/api/trades?limit=50`

거래 히스토리 조회

**응답:**
```json
{
  "trades": [
    {
      "market_id": "...",
      "market_question": "Bitcoin Up or Down",
      "action": "ENTER_YES",
      "side": "YES",
      "price": 0.450,
      "size": 10,
      "position_after": 10,
      "avg_price": 0.450,
      "timestamp": 1737388800
    }
  ],
  "total": 15
}
```

### GET `/api/events?limit=100`

이벤트 로그 조회

**응답:**
```json
{
  "events": [
    {
      "type": "trade",
      "timestamp": 1737388800,
      "data": {...}
    },
    {
      "type": "signal",
      "timestamp": 1737388799,
      "data": {...}
    }
  ],
  "total": 150
}
```

### POST `/api/control`

봇 제어

**요청:**
```json
{
  "action": "start"  // "start", "stop", "pause"
}
```

**응답:**
```json
{
  "status": "started"
}
```

### WebSocket `/ws`

실시간 이벤트 스트림

**메시지 형식:**
```json
{
  "type": "trade_executed",
  "data": {
    "market_id": "...",
    "action": "ENTER_YES",
    ...
  }
}
```

---

## 💻 커스터마이징

### 포트 변경

`btc_web_server.py` 마지막 줄:

```python
uvicorn.run(app, host="0.0.0.0", port=8000)
```

→ 다른 포트로 변경 (예: 3000)

### UI 스타일 수정

HTML 템플릿은 `btc_web_server.py`의 `get_dashboard()` 함수 내부에 있습니다.

**Tailwind CSS** 사용:
- `bg-gray-900` - 배경색
- `text-green-400` - 텍스트 색상
- `rounded-lg` - 둥근 모서리

**Vue.js** 사용:
- `{{ status.running }}` - 데이터 바인딩
- `@click="controlBot('start')"` - 이벤트 핸들러

### 추가 기능 구현

#### 1. Telegram 알림 추가

거래 체결 시 Telegram 메시지 전송:

```python
async def on_trade_executed(trade_info: dict):
    # 기존 코드...

    # Telegram 전송
    await send_telegram_message(
        f"🎯 Trade: {trade_info['action']} "
        f"{trade_info['side']} x{trade_info['size']} "
        f"@ {trade_info['price']:.3f}"
    )
```

#### 2. 차트 추가

Chart.js로 PnL 차트 표시:

```html
<canvas id="pnlChart"></canvas>

<script>
const ctx = document.getElementById('pnlChart');
const chart = new Chart(ctx, {
  type: 'line',
  data: {
    labels: [], // 시간
    datasets: [{
      label: 'PnL',
      data: [], // PnL 데이터
      borderColor: 'rgb(75, 192, 192)',
    }]
  }
});
</script>
```

#### 3. 소리 알림

거래 체결 시 소리 재생:

```javascript
handleWebSocketMessage(msg) {
  if (msg.type === 'trade_executed') {
    // 소리 재생
    const audio = new Audio('/static/trade-sound.mp3');
    audio.play();

    // 기존 코드...
  }
}
```

---

## 🔒 보안 설정

### 1. 로컬 네트워크만 허용

`btc_web_server.py`:

```python
uvicorn.run(app, host="127.0.0.1", port=8000)
```

이제 localhost에서만 접속 가능

### 2. 인증 추가

FastAPI 의존성으로 간단한 API 키 인증:

```python
from fastapi import Header, HTTPException

async def verify_api_key(x_api_key: str = Header(...)):
    if x_api_key != "your-secret-key":
        raise HTTPException(status_code=403)

@app.get("/api/status", dependencies=[Depends(verify_api_key)])
async def get_status():
    ...
```

### 3. HTTPS 사용

Nginx 리버스 프록시로 HTTPS:

```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location / {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

---

## 📱 모바일 접속

웹 UI는 반응형이므로 모바일에서도 사용 가능:

1. PC와 같은 WiFi에 연결
2. PC의 IP 주소 확인:
   ```bash
   # Mac/Linux
   ifconfig | grep inet

   # Windows
   ipconfig
   ```
3. 모바일 브라우저에서:
   ```
   http://192.168.1.XXX:8000
   ```

---

## 🐛 문제 해결

### "Cannot connect to WebSocket"

**원인**: 방화벽 차단

**해결**:
```bash
# Mac
sudo ufw allow 8000

# Windows
Windows Defender 방화벽 → 인바운드 규칙 → 8000 포트 허용
```

### "Address already in use"

**원인**: 포트 8000이 이미 사용 중

**해결**:
```bash
# 프로세스 찾기
lsof -i :8000

# 종료
kill -9 <PID>

# 또는 다른 포트 사용
uvicorn.run(app, host="0.0.0.0", port=8001)
```

### "Data not updating"

**원인**: 봇이 실행 중이 아님

**해결**:
1. 웹 UI에서 "▶ Start" 버튼 클릭
2. 또는 `.env`에서 설정 확인
3. 로그 확인: 터미널에 에러 메시지 있는지

### "Too many WebSocket connections"

**원인**: 여러 탭에서 동시 접속

**해결**:
- 하나의 탭만 사용
- 또는 연결 제한 늘리기 (서버 코드 수정)

---

## 🎨 스크린샷 예시

### 대시보드 메인 화면
```
┌─────────────────────────────────────────────────────────────┐
│ 🚀 BTC Scalping Bot                                         │
│ Real-time 15-minute market trading dashboard                │
├─────────────────────────────────────────────────────────────┤
│ Bot Status    BTC Price      Total PnL     Win Rate         │
│ 🟢 RUNNING    $90,913.99     +$12.50       66.7%            │
├─────────────────────────────────────────────────────────────┤
│ [▶ Start]  [⏹ Stop]  [🔄 Refresh]           🟢 Connected   │
├─────────────────────────────────────────────────────────────┤
│ 📊 Active Markets (2)                                       │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Bitcoin Up or Down - Jan 20, 1:15-1:30 ET              │ │
│ │ YES: 0.565  NO: 0.435                                   │ │
│ │ Position: YES 10 | PnL: +2.5% ($0.25)                  │ │
│ │ Time Left: 12m 30s                                      │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ 💰 Recent Trades                                            │
│                                                             │
│ EXIT | YES x10 @ 0.525 ........................... +$0.75  │
│ ENTER_YES | YES x10 @ 0.450 ..................... +$0.00  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎓 다음 단계

1. **차트 추가** - PnL 그래프, BTC 가격 차트
2. **알림 설정** - Telegram, Discord, 이메일
3. **전략 설정 UI** - 웹에서 파라미터 조정
4. **백테스트 결과** - 과거 데이터 분석 표시
5. **다크/라이트 모드** - 테마 전환 기능

---

## 📚 참고 자료

- FastAPI 문서: https://fastapi.tiangolo.com
- Vue.js 가이드: https://vuejs.org/guide
- Tailwind CSS: https://tailwindcss.com
- WebSocket API: https://developer.mozilla.org/en-US/docs/Web/API/WebSocket

---

**Happy Trading with Web UI! 🚀📊**
